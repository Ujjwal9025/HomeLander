import { useEffect } from "react";
import { useDispatch } from "react-redux"

const HLAuth = () => {
  const dispatch = useDispatch();

  useEffect(() => {
    
  }, [dispatch])
  
  return (
    <div>HLAuth</div>
  )
}

export default HLAuth